<?php

namespace app\model;

class TestModel extends \core\Model
{
    protected $table = 'user';
    public function run()
    {
        $product = $this->findAll();
        var_dump($product);
    }
}